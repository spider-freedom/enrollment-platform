# 新疆大学招生宣传平台 — 实训小组任务分配

> 四人小组，按功能模块垂直划分，每人负责对应模块的全栈开发（后端 Java + 前端 Vue）。

---

## 👤 成员一：系统底座与公共端

### 后端负责

| 模块 | 文件 |
|---|---|
| **安全框架** | `security/JwtAuthFilter.java`、`security/JwtUtils.java`、`security/SecurityConfig.java`、`security/SecurityUtils.java` |
| **认证与用户** | `modules/system/controller/AuthController.java`（登录/注册）、`modules/system/controller/UserController.java`（个人资料/改密码/头像上传）、`modules/system/controller/CollegeUserController.java`（学院用户管理）、`modules/system/controller/SchoolUserController.java`（学校用户管理） |
| **用户服务** | `modules/system/service/UserService.java`、`modules/system/dto/` 全部 5 个 DTO |
| **公共基础设施** | `common/ApiResponse.java`、`common/BusinessException.java`、`common/PageResult.java`、`common/GlobalExceptionHandler.java`、`common/ExcelExportUtil.java` |
| **配置层** | `config/CorsConfig.java`、`config/MetaObjectHandlerConfig.java`、`config/MybatisPlusConfig.java`、`config/WebMvcConfig.java` |
| **实体层** | `entity/` 全部 7 个实体（Activity、Enrollment、Feedback、FeedbackAttachment、User、Policy、ActivityFieldConfig） |
| **持久层** | `mapper/` 全部 7 个 Mapper |
| **启动类** | `EnrollmentApplication.java` |

### 前端负责

| 模块 | 页面/文件 |
|---|---|
| **框架层** | `App.vue`、`layouts/MainLayout.vue`（主布局/导航/面包屑/菜单） |
| **路由与状态** | `router/index.ts`、`stores/user.ts`（Pinia 状态管理） |
| **基础设施** | `api/request.ts`（axios 拦截器/请求封装）、`types/index.ts`（TypeScript 类型定义）、`utils/api.ts`（响应解析工具）、`utils/constants.ts`（常量/状态映射/`canEnroll`） |
| **公共页面** | `LoginView.vue`（登录）、`RegisterView.vue`（注册） |
| **首页与政策** | `HomeView.vue`（首页 Banner/活动卡片）、`PolicyView.vue`（招生政策列表） |
| **信息展示页** | `AboutView.vue`（项目介绍）、`MajorsView.vue`（专业介绍） |
| **个人主页** | `ProfileView.vue`（个人信息展示/编辑/改密码/头像上传） |

---

## 👤 成员二：活动管理与报名流程（学生端 + 教师端）

### 后端负责

| 模块 | 文件 |
|---|---|
| **活动模块** | `modules/activity/controller/ActivityController.java`、`modules/activity/service/ActivityService.java`、`modules/activity/dto/` 全部 3 个 DTO（ActivityVO、ActivityRequest、ActivityListQuery） |
| **报名模块** | `modules/enrollment/controller/EnrollmentController.java`、`modules/enrollment/service/EnrollmentService.java`、`modules/enrollment/dto/` 全部 3 个 DTO（EnrollmentVO、EnrollmentRequest、EnrollmentQuery） |

### 前端负责

| 模块 | 页面/文件 |
|---|---|
| **教师活动浏览** | `TeacherActivityList.vue`（活动列表/卡片/筛选/报名人数显示） |
| **教师活动详情** | `TeacherActivityDetail.vue`（活动详情/报名弹窗/已报名检测/报名提交） |
| **教师报名记录** | `TeacherEnrollments.vue`（已报名活动列表/状态/撤回/反馈入口） |
| **学生活动浏览** | `StudentActivityList.vue`（活动列表/卡片/筛选/报名人数显示） |
| **学生活动详情** | `StudentActivityDetail.vue`（活动详情/报名弹窗/已报名检测/报名提交） |
| **学生报名记录** | `StudentEnrollments.vue`（已报名活动列表/状态/撤回/反馈入口） |
| **学生仪表盘** | `StudentDashboard.vue`（学生首页/统计概览） |

---

## 👤 成员三：反馈管理与审批工作流（学院管理端）

### 后端负责

| 模块 | 文件 |
|---|---|
| **反馈模块** | `modules/feedback/controller/FeedbackController.java`（学生/教师提交反馈/我的反馈/学院列表）、`modules/feedback/controller/AttachmentController.java`（附件上传） |
| **反馈服务** | `modules/feedback/service/FeedbackService.java`（提交/列表/回复/详情/统计）、`modules/feedback/service/AttachmentService.java` |
| **反馈 DTO** | `modules/feedback/dto/` 全部 4 个 DTO（FeedbackVO、FeedbackSubmitRequest、FeedbackQuery、FeedbackReplyRequest） |
| **审批工作流** | `modules/workflow/controller/ApprovalController.java`（学院审批/学校审批/通过/拒绝/批量操作/统计）、`modules/workflow/service/ApprovalService.java` |
| **审批 DTO** | `modules/workflow/dto/` 全部 5 个 DTO（ApprovalVO、ApprovalRequest、ApprovalStatsVO、BatchApprovalRequest） |

### 前端负责

| 模块 | 页面/文件 |
|---|---|
| **学生反馈提交** | `StudentFeedbackSubmit.vue`（反馈表单/评分/内容/活动信息获取/提交） |
| **学生反馈列表** | `StudentMyFeedback.vue`（我的反馈/评分展示/回复展示/列表状态） |
| **教师反馈提交** | `TeacherFeedbackSubmit.vue`（院系自动绑定/反馈表单/提交） |
| **教师反馈列表** | `TeacherMyFeedback.vue`（我的反馈/活动名称/回复展示） |
| **学院活动管理** | `CollegeActivityList.vue`（学院活动列表/活动创建编辑） |
| **学院审批管理** | `CollegeApprovalList.vue`（报名审批/通过/拒绝/批量操作/AI审批建议/统计卡片） |
| **学院反馈管理** | `CollegeFeedbackList.vue`（反馈列表/筛选/逐条AI分析/回复/评分展示/统计卡片） |
| **学院用户管理** | `CollegeUserManagement.vue`（用户列表/导入/禁用/重置密码/提为管理员） |

---

## 👤 成员四：数据统计与 AI 分析（学校管理端）

### 后端负责

| 模块 | 文件 |
|---|---|
| **统计模块** | `modules/statistics/controller/StatisticsController.java`、`modules/statistics/service/StatisticsService.java`、`modules/statistics/dto/` 全部 3 个 DTO（DashboardVO、CollegeStatVO、TrendVO） |
| **AI 模块** | `controller/AiController.java`（反馈分析/学校名标准化/审批建议/AI对话）、`ai/FeedbackAnalyzer.java`（单条反馈分析/批量分析/总结）、`ai/SchoolNameNormalizer.java`（学校名智能补全）、`ai/AiConfig.java` |

### 前端负责

| 模块 | 页面/文件 |
|---|---|
| **校级仪表盘** | `SchoolDashboard.vue`（统计概览/ECharts可视化/报名趋势/学院对比/活动排行） |
| **校级活动管理** | `SchoolActivityList.vue`（全校活动列表/状态管理）、`SchoolActivityCreate.vue`（活动创建/编辑/字段配置/Banner设置） |
| **校级审批管理** | `SchoolApprovalList.vue`（校级审批/通过/拒绝/筛选/统计卡片/AI审批建议/导出Excel） |
| **校级反馈管理** | `SchoolFeedbackList.vue`（全校反馈列表/筛选/逐条AI分析/回复/统计/导出） |
| **校级用户管理** | `SchoolUserManagement.vue`（全校用户管理/角色变更/状态管理/密码重置） |
| **AI 助手** | `AIAssistantView.vue`（AI智能问答/流式对话/招生政策咨询） |
| **图表工具** | `utils/charts.ts`（ECharts 主题/工具函数） |
| **API 层** | `api/index.ts`（所有后端接口封装，含活动/报名/反馈/审批/AI/用户/统计） |
| **公共布局** | `layouts/PublicLayout.vue`（未登录态布局） |

---

## 📊 任务分配汇总

| 成员 | 后端核心模块 | 前端页面数 | 核心业务 |
|------|------------|-----------|---------|
| 成员一 | system / security / common / config / entity / mapper | ~11 页 + 框架层 | 系统底座、认证授权、注册登录、个人资料、公共页面 |
| 成员二 | activity / enrollment | ~7 页 | 活动浏览/详情、学生/教师报名流程、报名记录管理、学生仪表盘 |
| 成员三 | feedback / workflow (approval) | ~8 页 | 反馈提交/列表/回复、学院端审批管理、学院端反馈/用户管理 |
| 成员四 | statistics / ai | ~8 页 + API层 | 校级仪表盘/活动/审批/反馈/用户管理、AI分析/问答、数据可视化 |

---

## 🔄 模块间协作说明

- **成员一**是基础依赖方：实体、Mapper、工具类、JWT 安全框架由成员一提供，其他成员在其上开发业务。
- **成员二的报名模块**产生数据 → **成员三的审批模块**处理审批 → 状态变更后通知成员二的报名列表更新。
- **成员三的反馈模块**依赖成员二报名审批通过（反馈提交校验 APPROVED 状态）。
- **成员四的管理端**可查看全校数据（跨学院），审批和反馈列表覆盖所有学院范围。
- **成员四的 AI 模块**被成员三（学院端 AI 分析）和成员四（学校端 AI 分析）共同调用。
